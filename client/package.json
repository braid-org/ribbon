{
  "name": "ribbon-client",
  "description": "Ribbon client: a microblogging server; demonstrating Braid-HTTP",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "snowpack dev --reload --output stream --open none",
    "build": "snowpack build",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1"
  },
  "scripts-comments": {},
  "devDependencies": {
    "@snowpack/plugin-svelte": "^3.5.2",
    "@snowpack/plugin-typescript": "^1.2.1",
    "snowpack": "^3.0.11"
  },
  "dependencies": {
    "anchorme": "^2.1.2",
    "braidify": "^0.1.4",
    "dompurify": "^2.2.6",
    "svelte": "^3.32.1",
    "svelte-icons": "^2.1.0",
    "svelte-local-storage-store": "^0.1.4",
    "svelte-select": "^3.17.0"
  }
}
