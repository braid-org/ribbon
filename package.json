{
  "private": true,
  "workspaces": [
    "braidjs",
    "client",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^5.3.0",
    "typescript": "^4.1.3"
  },
  "prettier": {
    "quoteProps": "consistent"
  },
  "scripts": {
    "start": "concurrently 'yarn workspace braidjs start' 'yarn workspace braidjs-server-example start' 'yarn workspace braidjs-client-example start'"
  }
}
