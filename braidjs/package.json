{
  "name": "braidjs",
  "description": "Braid protocol support for javascript",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc --outDir dist src/index.ts",
    "start": "tsc --outDir dist -w src/index.ts"
  },
  "scripts-comments": {
    "build": "Compiles typescript in src/ to plain js in dist/",
    "start": "Watch for typescript files in src/ and compile to plain js in dist/"
  },
  "devDependencies": {
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
